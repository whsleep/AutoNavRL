# 代码流程

## 参考

[强化学习快速上手：编写自定义通用gym环境类+主流开源强化学习框架调用](https://zhuanlan.zhihu.com/p/33553076)

[如何在 Gym 中注册自定义环境？](https://zhuanlan.zhihu.com/p/658888878)

[Stable Baselines/用户向导/矢量化环境](https://blog.csdn.net/The_Time_Runner/article/details/98052769)

## 相关库 `API` 及简要说明

[argparse](https://docs.python.org/zh-cn/3.15/howto/argparse.html)

[gym](https://www.gymlibrary.dev/)

[stable-baselines3](https://stable-baselines3.readthedocs.io/en/master/)

## `gym` 创建自定义仿真环境

在 `train.py` 中，声明 `RobotNavEnv` 类，它是`gym.Env`的子类，一般包括以下内容

### 🌟\_\_init\_\_(self)

主要内容为初始化动作空间与状态空间。

```python
def __init__(self):
    # 调用父类gym.Env的构造函数
    super(RobotNavEnv, self).__init__()
    # 初始化action空间
    ----
    # 初始observation空间
    ----
    pass
```

### 🌟step(self, action)

用于编写智能体与环境交互的逻辑，它接受`action`的输入，返回一个步长处理后的信息，一般会包含`reward`,以及其他的一些状态信息。

```python
def step(self, action):
    # 主要返回状态和奖励
    return self.state, reward, done, {}
```

### 🌟reset()：

在每轮开始之前重置环境信息和智能体的状态，一般会返回重置后的状态。

```python
def reset(self):
    # 返回重置后的状态
    return self.state
```

### 具体解读

#### `__init__(self)`

**具体可看代码，这里只列出关键信息**

这里定义 `action`的类型和最大最小值，由于在 `ir-sim`配置为`omni`底盘，所以这里`action`设定为二维并根据实车动力学上限进行限制即可。

```python 

    self.state_dim = 49  # 观测空间的维度
    self.max_steps = 150  # 每个episode的最大步数
        
    # 定义动作空间(线速度和角速度)
    self.action_space = spaces.Box(
        low=np.array([-0.6, -1.2]),  # [最小线速度, 最小角速度]
        high=np.array([0.6, 1.2]),   # [最大线速度, 最大角速度]
        dtype=np.float32
    )
        
    # 定义观测空间(归一化到[-1, 1])
    self.observation_space = spaces.Box(
        low=-1, 
        high=1, 
        shape=(self.state_dim,), 
        dtype=np.float32
    )
```

--- 

这里的 `sim` 为 `sim.py` 中根据 `ir-sim` 自定义的二维仿真环境。

`sim`中主要包含 `__init__`、`step`和`reset`三个函数，仍然是为了匹配`gym`提供的三个接口。具体的`sim.py`内容请自行阅读代码。

```python
from sim import SIM_ENV

    # 初始化模拟器
    self.sim = SIM_ENV(render=render)
```

---

下面这几步用于初始化智能体的`state`和环境信息，

```python
# 初始化episode跟踪变量
self._reset_episode_tracking()
# 获取初始观测值
initial_data = self.sim.reset()
self.current_obs, _ = self.prepare_state(initial_data)
```

`_reset_episode_tracking` 重置几个关键指标。

```python
self.time = 0 # 仿真离散时间
self.last_position = None # 上次位置
self.total_distance = 0 # 运行的路径长度
self.total_velocity = 0 # 运行的全部速度标量
self.steps = 0 
```

后面的流程主要是获取`49`维度的`current_obs`。

#### `step(self, action)`

明显这里的输入为一个 `action` 信息，`step` 主要根据`action` 推算出先一步的环境、状态、奖励等信息。

首先对`action`进行死区处理，由于电机死区的存在，较小的`action`实际不会得到智能体的正确响应。所以当`action`过小时，将其置为0。

```python
lin_velocity = 0 if abs(action[0]) < 0.15 else action[0]
ang_velocity = 0 if abs(action[1]) < 0.15 else action[1]
```

接下来使用 `ir-sim`仿真的前向模拟，这里贴出`sim.py`的`step`相关代码进行说明

首先调用 `ir-sim`的`step`进行前向模拟，这时会更新环境信息和智能体的当前状态。

```python
self.env.step(action_id=0, action=np.array([[lin_velocity], [ang_velocity]]))
```

---

获取传感器信息和智能体状态信息，

`scan`结构为[LaserScan Message](https://docs.ros.org/en/melodic/api/sensor_msgs/html/msg/LaserScan.html)

`robot_state` 为 $[x,y,\theta]$

```python
scan = self.env.get_lidar_scan()
robot_state = self.env.get_robot_state()
```

---

然后根据状态计算相关指标用于构建`reward`

`goal_vector`当前`state`指向目标点的矢量；

`distance`当前位置距离目标点的距离；

`diff_rad`当前方位角与目标点的差值；

`cos`, `sin`方向矢量和目标向量之间的正弦余弦值；

```python
goal_vector, distance, cos, sin, diff_rad = self._calculate_robot_metrics(robot_state)
```

---

后续作了更为详细的状态更新，计算相邻两状态的距离差值`distance_delta`和方位角差值`delta_rad`，

以及一些关键状态信息

是否抵达目标点 `goal`

是否发生碰撞 `collision`

```python
# 计算机器人状态和目标的距离和角度偏差
if self.prev_distance is None:
    distance_delta = delta_rad = 0
else:
    distance_delta = self.prev_distance - distance # 相邻两次步长差值
    delta_rad = abs(self.prev_diff_rad) - abs(diff_rad) # 向量两次方向角差值
        
# 更新上次距离和角度差
self.prev_distance = distance
self.prev_diff_rad = diff_rad
# 获取状态并计算奖励
goal = self.env.robot.arrive
collision = self.env.robot.collision
action = [lin_velocity, ang_velocity]
```

---

使用收集的信息进行`reward`的计算

```python
if goal:
    return 100.0
elif collision:
    return -100.0
        
# 奖励组件
progress_reward = distance_delta * 10  # 对接近目标的奖励
dir_progress = delta_rad * 1  # 与目标保持一致的奖励
time_penalty = -0.65  # 每个时间步的罚款
rotation_penalty = -abs(action[1]) * 0.4  # 过度旋转的处罚
        
# 避障
safe_distance = 1.35 # 安全距离
min_dist = min(laser_scan) # 雷达最近距离
# 安全距离大于最小距离惩罚项为0，否则为 安全距离减去最小距离
obstacle_penalty = -(safe_distance - min_dist) if min_dist < safe_distance else 0
        
return progress_reward + time_penalty + obstacle_penalty + dir_progress + rotation_penalty
```

---

最后返回所有状态信息和`reward`。

---

现在回到 `train.py`中的 `step`函数，这里重点在 `prepare_minstate`函数对数据的处理。

在该函数中首先对距离无穷大点云进行限制，即强行赋值为传感器的最大检测值。

```python
# 处理激光扫描中的无穷大值
inf_mask = np.isinf(latest_scan)
latest_scan[inf_mask] = 10 # 无穷大值设置为雷达的最远检测距离
```

接下来时对激光扫描的降维处理，只使用`self.state_dim - 7`也就是$49-7$维状态表示雷达检测的环境信息。所以将激光均雷达分为$42$个区域进行转换。

```python
# 下采样激光扫描数据 
max_bins = self.state_dim - 7
# 将2D点云按照扇形区域进行划分
bin_size = int(np.ceil(len(latest_scan) / max_bins))

min_values = []
# 创建扇形范围并获取最小值
for i in range(0, len(latest_scan), bin_size):
    bin = latest_scan[i : i + min(bin_size, len(latest_scan) - i)]
    # 找到当前扇形中的最小值并将其追加到min_values列表中
    min_values.append(min(bin) / 10)
```

最后返回`49`维的`observation`和状态标志`terminal`。

跳出`prepare_minstate`函数后，会进行一些指标计算，如`total_distance`等。

最终返回`observation`,`reward`还有一些关键信息。

#### `reset(self)`

首先是调用`sim.py`中`reset`函数

更新相关信息。


## '__main__'

### 读取命令行参数

| param      | Description |
| ----------- | ----------- |
| num_envs           | 并行训练环境的数量      |
| total-timesteps    | 训练的总时间步数        |
| model-path         | 保存/加载模型的路径     |
| tensorboard-log    | Tensorboard日志目录    |
| eval-episodes      | 评估的episode数量      |
| render             | 训练期间启用渲染        |


### 创建矢量化环境

```python
from stable_baselines3.common.vec_env import SubprocVecEnv, DummyVecEnv
    env = SubprocVecEnv(env_fns)
```

### 创建或加载`TD3`模型

```python
from stable_baselines3 import TD3
    try:
        model = TD3.load(args.model_path, env=env)
        print(f"从{args.model_path}加载现有模型")
    except:
        model = TD3("MlpPolicy", env, verbose=1, tensorboard_log=args.tensorboard_log)
        print("创建新模型")
```

### 训练

### 评估

### 保存